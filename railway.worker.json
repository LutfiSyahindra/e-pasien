{
    "$schema": "https://railway.com/railway.schema.json",
    "deploy": {
        "startCommand": "php artisan queue:work --sleep=3 --tries=3 --timeout=600",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}
